# Villager Integration Review Log — v0.12.x

**Sprint start:** 2026-05-04
**Reviewer:** Explorer agent (claude/cli-explorer-session-J3WqA)
**Executor branch:** `claude/sonnet-onboarding-oMP6A`
**Target branch:** `claude/cli-explorer-session-J3WqA` → merged into `dev`

Predecessor log (v0.10.30, 9 merges):
`team/humans/dinis_cruz/claude-code-web/05/01/v0.10.30/10__villager-integration-review-log.md`

---

## Baseline (2026-05-04)

- **Tests:** 2,760 passing, 0 failed, 0 skipped
- **Coverage:** 98% (10,226 lines measured, 249 uncovered)
- **Runtime:** ~258s warm serial
- **Branch:** synced to `dev` at `1f481fd`
- **Pack:** `team/villager/v0.12.x__perf-brief-pack/` (B01–B15; B01 runs first)

---

## Standing review checklist (applied on every merge)

- [ ] No multi-paragraph **class or method** docstrings — one line max (module-level docstrings are fine)
- [ ] No bare `ClassName()` instantiations just to call a utility method
- [ ] No duplicated helper methods across layers
- [ ] No mocks / patches / monkeypatch (`unittest.mock`, `MagicMock`, `@patch`) — use real objects
- [ ] No `__init__.py` files under `tests/`
- [ ] No Type_Safe violations: raw `str`/`int`/`dict` fields in data classes
- [ ] Production code changes are within brief scope (flag any out-of-scope changes)
- [ ] `stat` imported where `os.chmod` with `stat.S_*` constants is used
- [ ] Suite green after every fix commit before merging to dev
- [ ] Executor must NOT modify `team/humans/dinis_cruz/claude-code-web/` — Explorer owns it

---

## Merge 1 — B01 (instrumentation tools)

**Commits merged:** `e05a1a6` (B01 closeout) through `2d2a254` (profile clone) — 7 commits
**My fix commit:** see below
**Test count:** 2,760 → **2,818** (+58 tests, all passing)

### What the executor implemented

Five Phase-0 instrumentation tools under `sgit_ai/cli/dev/`, registered as `sgit dev <…>`:

| Tool | Class | Schema |
|------|-------|--------|
| `sgit dev profile clone` | `Dev__Profile__Clone` | `Schema__Profile__Clone` + `__Phase` |
| `sgit dev tree-graph` | `Dev__Tree__Graph` | `Schema__Tree__Graph` + `__Commit` + `__DepthLevel` |
| `sgit dev server-objects` | `Dev__Server__Objects` | `Schema__Server__Objects` + `__TypeCount` |
| `sgit dev step-clone` | `Dev__Step__Clone` | `Schema__Step__Clone` + `__Event` |
| `sgit dev replay` | `Dev__Replay` | `Schema__Replay` + `__Phase__Diff` |

Wired via `CLI__Dev` registered in `CLI__Main`. The only `CLI__Main.py` changes: added
`CLI__Dev` field + `dev` sub-parser registration + `'dev'` added to `_NO_WALK_UP`.
No modification to any vault sync / crypto path. All tools use real `Vault__API__In_Memory`
and real temp dirs — no mocks. All schemas pass round-trip invariant.

58 tests across 5 test files (10–13 per tool).

### Fixes applied

**`Dev__Step__Clone.py` `step_clone` — 3-line method docstring:** trimmed to one line.

**`Schema__Server__Objects.py` `hot_tree_ids` — raw `list` field (Type_Safe violation):**
`hot_tree_ids : list` → `hot_tree_ids : list[Safe_Str__Object_Id]`. Raw `list` is a
violation; `list[Safe_Str]` would have been actively harmful — `Safe_Str` replaces
hyphens with underscores, silently corrupting `obj-cas-imm-*` IDs. `Safe_Str__Object_Id`
is the correct type (validates `^obj-cas-imm-[0-9a-f]{12}$`, preserves hyphens).
Round-trip test fixture updated to use a real object ID (`obj-cas-imm-aabb11223344`).
Full debrief: `team/explorer/dev/debriefs/05/04/b01-review-fixes.md`.

### Scope check

`CLI__Main.py` change is minimal and within B01's explicit scope ("register a minimal
`dev` parser locally and let B02 fold it in"). No main-command behaviour changed.

---

## Merge 2 — B07 (case-study diagnosis)

**Commits merged:** `fac4dd4` (regression tests), `5811461` (diagnosis doc) — 2 commits (plus merge)
**My fix commit:** none required — clean delivery
**Test count:** 2,818 → **2,834** (+16 tests, all passing)

### What the executor implemented

**Diagnosis report:** `team/villager/v0.12.x__perf-brief-pack/changes__case-study-diagnosis.md`

Five-hypothesis verdict against a 43-commit / 330-tree synthetic vault:

| Hypothesis | Verdict | Key number |
|---|---|---|
| H1 — many small BFS waves | MINOR | 3–4 waves, avg batch ~600 objects — waves are large |
| H2 — decrypt/parse overhead | MINOR | 0.79 ms/tree → ~1.9s for 2,375 trees (1% of 184s) |
| H3 — random-IV no-dedup | **PRIMARY CAUSE** | Synthetic 5.35× dedup; real vault ~1.0× (all objects unique) |
| H4 — server-side S3 GETs per object | LIKELY SIGNIFICANT | ~120s estimated; unverifiable without backend |
| H5 — walking history unnecessary | **CONFIRMED lever** | HEAD-only = 12.7% of trees; 87% waste |

Root cause: old vaults used random-IV AES-GCM → every tree re-encrypted per commit →
`visited_trees` set cannot dedup → 2,375 downloads instead of ~300.

Recommended fix priority: H5+B08 (HEAD-only pack, ~40–100× speedup) → H3 migration (B10, ~7–8×) → H4 (B08 batching).

**Fixtures:** `tests/fixtures/perf/case-study-{clone-baseline,tree-graph,server-objects}.json`
+ `build_case_study.py` (reproducible build script).

**Bonus:** `test_Perf__Case_Study__Vault.py` (16 tests) — structural regression suite
validating H1–H5 automatically. Beyond strict brief scope but accepted as high-value addition.

### Fixes applied

None — clean delivery.

---

## Merge 3 — B02 (CLI namespace restructure)

**Commits merged:** `c122a91` — 1 commit
**My fix commit:** `ced6038` (shared with B03/B04/B05, see below)
**Test count:** 2,834 → **2,953** (accumulated across B02–B05; all 119 new tests passing)

### What the executor implemented

Full CLI namespace restructure per design D2. Moved ~50 top-level commands into 7 namespaces:

| Namespace class | Commands |
|---|---|
| `CLI__History` | `diff`, `show`, `log`, `revert`, `reset` |
| `CLI__File` | `cat`, `ls`, `write` |
| `CLI__Inspect` | `inspect-tree` → `inspect tree`, `inspect-object`, `inspect-stats`, `diff-state` |
| `CLI__Check` | `fsck`, `verify`, `sign` |
| `CLI__Branch` | `branch list/create/switch/delete/rename` |
| `CLI__Vault` | `probe`, `delete-on-remote`, `rekey`, `uninit`, `clean`, `share`, `info` |
| `CLI__Dev` | `dump`, `cat-object`, `derive-keys`, `debug` (+ existing B01 tools) |

`_RENAME_MAP` dict in `CLI__Main` provides friendly "command has moved to X" errors for all old invocations. Inventory doc at `changes__cli-inventory.md`.

### Fixes applied

See shared fix commit below (applies to B02–B05 together).

---

## Merge 4 — B03 (clone family + `create`)

**Commits merged:** `c45dae8` — 1 commit
**My fix commit:** `ced6038` (shared)

### What the executor implemented

New top-level commands:
- `sgit create <vault-name>` — one-shot init + remote create + publish; wired to real in-memory API
- `sgit clone-branch` / `sgit clone-headless` / `sgit clone-range` — stubs with pointer to B09
- `--bare` flag on `clone`, `clone-branch`, `clone-range`; `clone-headless --bare` errors friendly

Tests: `test_CLI__Create.py`, `test_CLI__Clone_Family.py`.

### Fixes applied

See shared fix commit below.

---

## Merge 5 — B04 (context-aware visibility)

**Commits merged:** `60195fc` — 1 commit
**My fix commit:** `ced6038` (shared)

### What the executor implemented

`Vault__Context` detector with three states (`OUTSIDE`, `INSIDE_WORKING`, `INSIDE_BARE`).
Walks parent dirs looking for `.sg_vault/`; reads `config.json` for vault ID.
`detect_with_override` honours `--vault PATH` global flag.
Tests: `test_Vault__Context.py`.

### Fixes applied

See shared fix commit below.

---

## Merge 6 — B05 (workflow framework)

**Commits merged:** `4e157fb` — 1 commit
**My fix commit:** `ced6038` (shared with B02–B05)

### What the executor implemented

Full workflow framework under `sgit_ai/workflow/` and `sgit_ai/schemas/workflow/`:

| Class | Role |
|---|---|
| `Step` | Base class — `name`, `input_schema`, `output_schema`, `execute()`, `is_done()` |
| `Workflow` | Base class — `name`, `version`, `steps`, `execute()` |
| `Workflow__Workspace` | On-disk state; atomic writes via temp+rename |
| `Workflow__Runner` | Orchestrator — manifest tracking, resume, version guard, transaction log |

Transaction log (design D7) emitted on every run; mode `off` by default until B14 wires config.

CLI: `sgit dev workflow list/show/run/resume/inspect/gc/log`.

Tests: 4 test files, synthetic 3-step fixture covering happy path, resume, abort, version guard, atomic write.

### Fixes applied (shared fix commit `ced6038` covers B02–B05)

**`Workflow__Workspace._atomic_write` — `@staticmethod` violation:**
CLAUDE.md bans `@staticmethod`. Removed decorator; method signature gains `self` (callers already used `self._atomic_write(…)` so no call-site changes needed).

**`Workflow__Runner._now_iso` / `_now_ms` — module-level functions:**
Two helper functions at the bottom of `Workflow__Runner.py` violate "no module-level functions". Moved into the class as private instance methods; 6 call sites updated to `self._now_iso()` / `self._now_ms()`.

**`CLI__Dev__Workflow` — 3 module-level functions + 1 module-level `dict`:**
`register_workflow`, `_known_workflows`, `_find_workspace`, and `_WORKFLOW_REGISTRY` were all at module level. Moved into `CLI__Dev__Workflow` as `@classmethod` / instance method + class-level `_registry = {}` attribute. Module-level aliases (`register_workflow = CLI__Dev__Workflow.register_workflow`, `_WORKFLOW_REGISTRY = CLI__Dev__Workflow._registry`) kept so existing test imports (`from … import register_workflow, _WORKFLOW_REGISTRY`) continue to work without changes.

**`test_Step.py`, `test_Workflow__Workspace.py`, `test_Synthetic__Workflow.py` — raw `str` fields in Type_Safe classes:**
7 test-fixture Type_Safe classes had `field : str = None` — Type_Safe violation. Changed to `field : Safe_Str = None`. `Safe_Str` sanitises hyphens → underscores, so test values containing hyphens (`'result-a'`, `'pre-done'`, `'step2-out'`) were updated to underscore equivalents. All 2,953 tests pass.

---

## Merge 7 — B06 (clone workflow refactor)

**Commits merged:** `6e5f4f6` — 1 commit
**My fix commit:** `fa66c04`
**Test count:** 2,953 → **2,953** (no new tests — see note below)

### What the executor implemented

Full refactor of `Vault__Sync__Clone._clone_with_keys` into a 10-step `Workflow__Clone` pipeline. Existing tests pass without modification (behaviour preserved).

**New files under `sgit_ai/workflow/clone/`:**

| Step | Class | Key work |
|---|---|---|
| 1 | `Step__Clone__Derive_Keys` | Derives vault_id, read_key, branch_index_file_id from vault_key |
| 2 | `Step__Clone__Check_Directory` | Validates target dir, creates bare vault structure |
| 3 | `Step__Clone__Download_Index` | Downloads and saves the branch index |
| 4 | `Step__Clone__Download_Branch_Meta` | Downloads all branch refs + public keys |
| 5 | `Step__Clone__Walk_Commits` | BFS commit chain walk, collects root_tree_ids |
| 6 | `Step__Clone__Walk_Trees` | BFS tree walk, saves all tree objects |
| 7 | `Step__Clone__Download_Blobs` | Downloads blob objects (skipped in sparse mode) |
| 8 | `Step__Clone__Create_Clone_Branch` | Creates local clone branch, writes pending registration |
| 9 | `Step__Clone__Extract_Working_Copy` | Checks out working tree from commit |
| 10 | `Step__Clone__Setup_Local_Config` | Writes vault_key + config.json |

Shared state: `Schema__Clone__State` (all fields typed with domain-specific `Safe_*` types). Non-serialisable manager objects held in `Clone__Workspace` (extends `Workflow__Workspace`; `object`-typed fields are a design necessity — `object` is not in the prohibited `str`/`int`/`float`/`dict` list).

**Bug fix (incidental):** `Safe_Str__Commit_Id` was inheriting `Safe_Str`'s default regex which stripped hyphens — silently corrupting `obj-cas-imm-*` IDs. Fixed: regex now `[^a-zA-Z0-9\-]`, `allow_empty = True` (branches can have no commits yet).

### Fixes applied

**`Schema__Clone__State` — multi-paragraph class docstring:** 3-line class docstring trimmed to one line.

### Missing tests — **flagged, not blocking**

B06 ships no test files. The brief requires at least 25 step-level tests + 3 workflow-level tests. Per Dinis: tests are being written in the next delivery. Merging on the strength of all 2,953 existing tests passing (behaviour preserved). **Test gap must be closed before this branch merges to `dev`.**

---

## Merge 8 — B06 tests + Safe_Str audit + B05 freeze doc

**Commits merged:** `e5944a5` — 1 commit
**My fix commit:** `1974d92`
**Test count:** 2,953 → **2,987** (+34 tests, all passing)

### What the executor implemented

**B06 tests (34)** — closes the gap flagged in Merge 7:

| Test file | Tests | Coverage |
|---|---|---|
| `test_Schema__Clone__State.py` | 9 | Round-trip, field defaults, commit ID round-trip, root_tree_ids list |
| `test_Clone__Workspace.py` | 9 | Manager lifecycle, ensure_managers idempotency, save_file, progress callback |
| `test_Workflow__Clone.py` | 16 | Structure (name, version, step count, step order, step names) + full pipeline (clone files, verify content, sparse mode) |

**`Safe_Str` → `Safe_Str__Id` audit** across 8 existing schemas: `Schema__Diff_Result`, `Schema__Dump_Branch`, `Schema__Dump_Commit`, `Schema__Dump_Object`, `Schema__Dump_Ref`, `Schema__Dump_Tree`, `Schema__Step__Clone`, `Schema__Tree__Graph`. `Safe_Str` strips hyphens; `Safe_Str__Id` (from `osbot_utils`) allows `[a-zA-Z0-9_-]` and preserves dashes — correct for `obj-cas-imm-*`, `ref-pid-*`, `branch-*` IDs. Verified: `Safe_Str__Id('obj-cas-imm-aabb11223344')` returns the value unchanged.

**B05 architect freeze doc:** `team/villager/v0.12.x__perf-brief-pack/changes__workflow-framework-spec.md` — locked API for `Step` / `Workflow` / `Workflow__Workspace`, atomic-write protocol, workspace lifetime, step granularity policy, schema versioning.

### Fixes applied

**`test_Workflow__Clone.py` — module-level function `_build_pushed_vault`:** Moved into `Test_Workflow__Clone__Full_Pipeline` as `@classmethod _build_snapshot`. `setup_class` calls `cls._build_snapshot(...)` instead.

---

## Merge 9 — B12 (storage layer restructure)

**Commits merged:** `3113189`, `f9deb23`, `e0983a1` — 3 commits
**My fix commit:** `a1e41f5`
**Test count:** 2,987 → **2,995** (+8 tests, all passing)

### What the executor implemented

**Layered architecture restructure** — created `sgit_ai/storage/` as a new architectural layer and moved 6 files into it:

| Old location | New location |
|---|---|
| `sgit_ai/objects/Vault__Object_Store.py` | `sgit_ai/storage/` |
| `sgit_ai/objects/Vault__Ref_Manager.py` | `sgit_ai/storage/` |
| `sgit_ai/objects/Vault__Commit.py` | `sgit_ai/storage/` |
| `sgit_ai/sync/Vault__Storage.py` | `sgit_ai/storage/` |
| `sgit_ai/sync/Vault__Branch_Manager.py` | `sgit_ai/storage/` |
| `sgit_ai/sync/Vault__Sub_Tree.py` | `sgit_ai/storage/` |

All 94 affected import sites updated (mechanical path change, zero logic changes). `LARGE_BLOB_THRESHOLD` constant moved from `sgit_ai/api/` into `storage/Vault__Sub_Tree.py` locally to break an upward import.

**Layer enforcement test** (`tests/unit/architecture/test_Layer_Imports.py`, 8 tests): AST-parses source files without executing them and asserts crypto / storage layer boundaries per design D6. One pre-existing violation tracked in `KNOWN_VIOLATIONS` (Vault__Crypto imports Simple_Token from transfer — inline import, fix deferred to B13).

**Architect decision record** (`changes__storage-move-plan.md`): documents which files moved, which were deferred (`Vault__Inspector` → B14, `Vault__Sync*` → B13), `secrets/` decision, `Vault__Key_Manager` location note.

### Scope check

B12 brief calls for the `storage` layer split. `Vault__Inspector` and all `Vault__Sync*` files deferred to B13/B14 as documented in the move plan — correct.

### Fixes applied

**`test_Layer_Imports.py` — 3 module-level functions:** `_collect_py_files`, `_sgit_ai_imports`, `_violation_key` moved into `Test_Layer_Imports` as instance methods; all call-sites updated to `self.*`.

---

## Merge 10 — B13 (core/network/pki layer restructure)

**Commits merged:** `1353523`–`54377c5` — 11 commits (+ merge conflict resolution)
**My fix commit:** `a059b1b`
**Test count:** 2,995 → **3,005** (+10 tests, all passing)

### What the executor implemented

Massive continuation of the D6 layered restructure:

| Restructure | Detail |
|---|---|
| `sgit_ai/sync/` → `sgit_ai/core/` + `sgit_ai/core/actions/*/` | `Vault__Sync__Base`, `Vault__Sync`, `Vault__Bare`, `Vault__Components`, `Vault__Context`, `Vault__Errors`, `Vault__Ignore`, `Vault__Remote_Manager` → `core/`; 14 action sub-classes (clone, pull, push, diff, dump, stash, gc, revert, fetch, merge, branch, commit, status, sparse, fsck, lifecycle, admin) each into their own `core/actions/<name>/` sub-folder |
| `sgit_ai/api/` + `sgit_ai/transfer/` → `sgit_ai/network/` | `network/api/` + `network/transfer/` sub-folders |
| `sgit_ai/pki/` → `sgit_ai/crypto/pki/` | PKI moved under crypto layer |

All import sites across CLI, tests, and workflow updated (mechanical path changes). Architect move plan doc added: `changes__core-network-relocation-plan.md`.

**Extended layer enforcement** (`test_Layer_Imports.py`): B13 added `network` and `core` layer rules to the enforcement test (4 new layers total: crypto, storage, network, core). Added `_check_layer` helper and `KNOWN_VIOLATIONS` entries for `Vault__Transfer.py` (imports storage — architectural note, deferred to future brief).

**Merge conflict:** `tests/unit/sync/test_Vault__Sync__Write_File__Guard.py` had import `sgit_ai.sync.Vault__Errors` vs B13's `sgit_ai.core.Vault__Errors` — resolved taking B13 path.

### Scope check

B13 brief: "move `sync/` → `core/actions/`, `api/+transfer/` → `network/`, `pki/` → `crypto/pki/`". All moves completed and import sites updated. `Vault__Inspector` still in `objects/` (deferred to B14 as per move plan). No logic changes.

### Pre-existing violations noted (not blocking — in code predating B13)

The explore agent flagged ~115 violations in moved code. Reviewing: all raw `str`/`int`/`bytes`/`dict` fields and multi-paragraph docstrings in `Vault__Batch`, `Vault__Transfer`, `Vault__Archive`, `Vault__Dump`, `Vault__Diff`, etc. are pre-existing (were in `sync/`, `api/`, `transfer/` before B13 and were never fixed in prior merges). Also `Dev__Server__Objects.py:_classify` and `Dev__Tree__Graph.py:_bfs_*` are pre-existing module-level function violations from B01. These are tracked for a future cleanup brief — not blocking B13 merge.

### Fixes applied

**`test_Layer_Imports.py` — 4 module-level functions (recurring):** Executor force-pushed B12 with module-level functions and built B13 on top. Functions `_collect_py_files`, `_sgit_ai_imports`, `_violation_key` (same as B12 fix), plus new `_check_layer` — all moved into `Test_Layer_Imports` as instance methods; all call-sites updated to `self.*`.

**`sgit_ai/core/Vault__Sync.py` — dead copy of `_pull_stats_line`:** A second (differently-formatted) copy of `_pull_stats_line` was left at module level in `Vault__Sync.py` and never called. Removed entirely.

**`sgit_ai/core/actions/pull/Vault__Sync__Pull.py` — `_pull_stats_line` at module level:** Moved into `Vault__Sync__Pull` as instance method `_pull_stats_line(self, ...)`. Updated 2 call sites to `self._pull_stats_line(...)`.

**`tests/unit/sync/test_Vault__Sync__Coverage.py`:** Was importing the deleted `_pull_stats_line` from `Vault__Sync`. Updated to import `Vault__Sync__Pull` and instantiate it; updated assertions to match the surviving format (`N commits, N trees, N blobs in Xs`).

---

## Merge 11 — B14 (plugin system)

**Commits merged:** `3cd4775` — 1 commit
**My fix commit:** `8e0a4f1`
**Test count:** 3,005 → **3,027** (+22 tests, all passing)

### What the executor implemented

**Plugin system** (`sgit_ai/plugins/`) — new top-level layer, D8 design:

| Component | Detail |
|---|---|
| `Plugin__Loader` | Discovers `plugin.json` manifests, filters by user config (`~/.sgit/config.json`), dynamically imports plugin classes |
| `Plugin__Read_Only` | Base class for all plugins; `manifest : Schema__Plugin_Manifest`, `register_subparsers()`, `commands()` |
| `Schema__Plugin_Manifest` | `name`, `version`, `stability`, `commands`, `enabled` |
| `plugins/check/` | `CLI__Check` + `Plugin__Check` + `plugin.json` |
| `plugins/dev/` | `CLI__Dev` (extended with plugins cmds) + `Plugin__Dev` + `plugin.json` |
| `plugins/file/` | `CLI__File` + `Plugin__File` + `plugin.json` |
| `plugins/history/` | `CLI__History` + `Plugin__History` + `plugin.json` |
| `plugins/inspect/` | `CLI__Inspect` + `Plugin__Inspect` + `plugin.json` |

`CLI__Main` updated to use `Plugin__Loader.load_enabled()` and register plugin subparsers dynamically. Dev CLI extended with `plugins list/show/enable/disable` commands.

**Layer enforcement** (`test_Layer_Imports.py`): B14 added `plugins` to the enforced layer set. Helper methods correctly inside `Test_Layer_Imports` class — no regression this time.

**Architect plan doc:** `changes__plugin-system-plan.md`.

### Scope check

B14 brief: implement plugin loader, 5 initial plugins, layer enforcement. Delivered completely. No out-of-scope changes.

### Fixes applied

**`Schema__Plugin_Manifest.py` — raw `str` and `list` fields:**
- Removed local `Safe_Str__Semver` duplicate (different regex from canonical); now imports `Safe_Str__Semver` from `sgit_ai.safe_types`.
- `stability : str = 'stable'` → `stability : Safe_Str = None`. Default `None` is correct — `Plugin__Loader.load_manifest()` always sets stability from JSON (with `'stable'` fallback).
- `commands : list` → `commands : list[Safe_Str__Plugin_Name]`.

**`test_Plugin__Dev__Plugins_Cmds.py` — module-level `_run` function:** Moved into `Test_Plugin__Dev__Plugins_Cmds` as instance method; updated 5 call sites to `self._run(...)`.

**`test_Schema__Plugin_Manifest.py`:** `test_default_enabled` expected `stability == 'stable'`; updated to `stability is None` to match the corrected schema default.

---

## Merge 12 — B15 (Workflow__Fetch / Workflow__Pull / Workflow__Push + shared step library)

**Commits merged:** `1c406b9` — 1 commit (+ executor sync merge)
**My fix commit:** `69767c5`
**Test count:** 3,027 → **3,068** (+41 tests, all passing)

### What the executor implemented

Three new workflow pipelines plus a shared step library:

| Workflow | Steps | Schema |
|---|---|---|
| `Workflow__Fetch` | 4: derive_keys → load_branch_info → fetch_remote_ref → fetch_missing | `Schema__Fetch__State` |
| `Workflow__Pull` | 5: +merge | `Schema__Pull__State` |
| `Workflow__Push` | 6: derive_keys → check_clean → local_inventory → fast_forward_check → upload_objects → update_remote_ref | `Schema__Push__State` |

**Shared step library** (`sgit_ai/workflow/shared/Keys__Derivation.py`): `derive_vault_keys` shared logic reused by all three Derive_Keys steps.

**Workspace classes:** `Fetch__Workspace`, `Pull__Workspace`, `Push__Workspace` — each extends `Workflow__Workspace` with domain-specific manager fields.

**Bonus docs:** `changes__00c__opus-mid-sprint-review.md` (Opus mid-sprint review) + `brief__08b__sg-send-backend-spec.md` (SG/Send backend spec for B08).

**Tests:** 4 test files — structure + integration tests for each workflow, plus shared step library test.

### Scope check

B15 brief: implement Workflow__Pull, Workflow__Fetch, Workflow__Push and the shared step library. All delivered. B08 integration stubs noted in workflow docstrings (pack-based upload deferred). Clean delivery.

### Fixes applied

**`Schema__Pull__State.py` — `merge_status : str = ''`:** Changed to `merge_status : Safe_Str = None`; added `Safe_Str` import. The step always sets this field explicitly; `None` default is correct. Updated `test_merge_status_default` to assert `None`.

**`Workflow__Fetch`, `Workflow__Pull`, `Workflow__Push` — multi-paragraph class docstrings:** Each had a multi-line docstring with blank lines. Trimmed to one descriptive line each.

---

## Merge 13 — B15 runtime bugfix + executor session briefing (2026-05-05)

**Source commits:** `2fdb282` (fix), `e86ddf0` (docs)
**My fix commit:** none required
**Test count:** 3,068 → **3,068** (unchanged — only runtime fix and docs)

### What the executor delivered

**Bug fix B15-1 (`2fdb282`):** `Step__Pull__Fetch_Missing.py` — `on_progress=` keyword arg changed to `_p=`. The method `_fetch_missing_objects` accepts `_p` not `on_progress`; without this fix any live pull would raise `TypeError`. The equivalent `Step__Fetch__Fetch_Missing.py` was already correct (`_p=`).

**Session update briefing (`e86ddf0`):** `team/villager/v0.12.x__perf-brief-pack/02__sonnet-session-update-2026-05-05.md` — comprehensive status doc covering B14/B15 scope deviations, two known bugs (B15-1 fixed, B04-1 open), remaining brief backlog (B08 → B10 → B06b priority order), and 7 proposals for future briefs. Read by reviewer.

### Scope check

No new production modules — single-line runtime fix + docs. Clean.

### Fixes applied

None — the bug was caught and fixed by the executor before the review merge.

### Known open items (carried forward from executor briefing)

- **B04-1:** `CLI__Main.run()` never calls `_detect_context()` — wrong-context guard exists but is unwired. Fix documented in briefing §4.
- **B19:** `read_key_hex : Safe_Str__Write_Key` type mismatch — should be a distinct `Safe_Str__Read_Key`.
- **B22:** `Workflow__Runner.run()` wraps all exceptions in `RuntimeError`, losing typed exceptions.
- **B15 signing key gap:** `Step__Pull__Merge.execute()` calls `workspace.vc.create_commit()` for merge commits without passing a signing/write key. The existing `Vault__Sync__Pull.pull()` loads the key from `key_manager`. This must be resolved before full B15 wiring.
- **B15 workflow CLI registration:** `CLI__Dev__Workflow` only knows `Workflow__Clone`; push/pull/fetch workflows not visible in `sgit dev workflow list`.
