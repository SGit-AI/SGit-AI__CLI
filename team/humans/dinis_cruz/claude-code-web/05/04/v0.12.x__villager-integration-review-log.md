# Villager Integration Review Log — v0.12.x

**Sprint start:** 2026-05-04
**Reviewer:** Explorer agent (claude/cli-explorer-session-J3WqA)
**Executor branch:** `claude/sonnet-onboarding-oMP6A`
**Target branch:** `claude/cli-explorer-session-J3WqA` → merged into `dev`

Predecessor log (v0.10.30, 9 merges):
`team/humans/dinis_cruz/claude-code-web/05/01/v0.10.30/10__villager-integration-review-log.md`

---

## Baseline (2026-05-04)

- **Tests:** 2,760 passing, 0 failed, 0 skipped
- **Coverage:** 98% (10,226 lines measured, 249 uncovered)
- **Runtime:** ~258s warm serial
- **Branch:** synced to `dev` at `1f481fd`
- **Pack:** `team/villager/v0.12.x__perf-brief-pack/` (B01–B15; B01 runs first)

---

## Standing review checklist (applied on every merge)

- [ ] No multi-paragraph **class or method** docstrings — one line max (module-level docstrings are fine)
- [ ] No bare `ClassName()` instantiations just to call a utility method
- [ ] No duplicated helper methods across layers
- [ ] No mocks / patches / monkeypatch (`unittest.mock`, `MagicMock`, `@patch`) — use real objects
- [ ] No `__init__.py` files under `tests/`
- [ ] No Type_Safe violations: raw `str`/`int`/`dict` fields in data classes
- [ ] Production code changes are within brief scope (flag any out-of-scope changes)
- [ ] `stat` imported where `os.chmod` with `stat.S_*` constants is used
- [ ] Suite green after every fix commit before merging to dev
- [ ] Executor must NOT modify `team/humans/dinis_cruz/claude-code-web/` — Explorer owns it

---

## Merge 1 — B01 (instrumentation tools)

**Commits merged:** `e05a1a6` (B01 closeout) through `2d2a254` (profile clone) — 7 commits
**My fix commit:** see below
**Test count:** 2,760 → **2,818** (+58 tests, all passing)

### What the executor implemented

Five Phase-0 instrumentation tools under `sgit_ai/cli/dev/`, registered as `sgit dev <…>`:

| Tool | Class | Schema |
|------|-------|--------|
| `sgit dev profile clone` | `Dev__Profile__Clone` | `Schema__Profile__Clone` + `__Phase` |
| `sgit dev tree-graph` | `Dev__Tree__Graph` | `Schema__Tree__Graph` + `__Commit` + `__DepthLevel` |
| `sgit dev server-objects` | `Dev__Server__Objects` | `Schema__Server__Objects` + `__TypeCount` |
| `sgit dev step-clone` | `Dev__Step__Clone` | `Schema__Step__Clone` + `__Event` |
| `sgit dev replay` | `Dev__Replay` | `Schema__Replay` + `__Phase__Diff` |

Wired via `CLI__Dev` registered in `CLI__Main`. The only `CLI__Main.py` changes: added
`CLI__Dev` field + `dev` sub-parser registration + `'dev'` added to `_NO_WALK_UP`.
No modification to any vault sync / crypto path. All tools use real `Vault__API__In_Memory`
and real temp dirs — no mocks. All schemas pass round-trip invariant.

58 tests across 5 test files (10–13 per tool).

### Fixes applied

**`Dev__Step__Clone.py` `step_clone` — 3-line method docstring:** trimmed to one line.

**`Schema__Server__Objects.py` `hot_tree_ids` — raw `list` field (Type_Safe violation):**
`hot_tree_ids : list` → `hot_tree_ids : list[Safe_Str__Object_Id]`. Raw `list` is a
violation; `list[Safe_Str]` would have been actively harmful — `Safe_Str` replaces
hyphens with underscores, silently corrupting `obj-cas-imm-*` IDs. `Safe_Str__Object_Id`
is the correct type (validates `^obj-cas-imm-[0-9a-f]{12}$`, preserves hyphens).
Round-trip test fixture updated to use a real object ID (`obj-cas-imm-aabb11223344`).
Full debrief: `team/explorer/dev/debriefs/05/04/b01-review-fixes.md`.

### Scope check

`CLI__Main.py` change is minimal and within B01's explicit scope ("register a minimal
`dev` parser locally and let B02 fold it in"). No main-command behaviour changed.

---

## Merge 2 — B07 (case-study diagnosis)

**Commits merged:** `fac4dd4` (regression tests), `5811461` (diagnosis doc) — 2 commits (plus merge)
**My fix commit:** none required — clean delivery
**Test count:** 2,818 → **2,834** (+16 tests, all passing)

### What the executor implemented

**Diagnosis report:** `team/villager/v0.12.x__perf-brief-pack/changes__case-study-diagnosis.md`

Five-hypothesis verdict against a 43-commit / 330-tree synthetic vault:

| Hypothesis | Verdict | Key number |
|---|---|---|
| H1 — many small BFS waves | MINOR | 3–4 waves, avg batch ~600 objects — waves are large |
| H2 — decrypt/parse overhead | MINOR | 0.79 ms/tree → ~1.9s for 2,375 trees (1% of 184s) |
| H3 — random-IV no-dedup | **PRIMARY CAUSE** | Synthetic 5.35× dedup; real vault ~1.0× (all objects unique) |
| H4 — server-side S3 GETs per object | LIKELY SIGNIFICANT | ~120s estimated; unverifiable without backend |
| H5 — walking history unnecessary | **CONFIRMED lever** | HEAD-only = 12.7% of trees; 87% waste |

Root cause: old vaults used random-IV AES-GCM → every tree re-encrypted per commit →
`visited_trees` set cannot dedup → 2,375 downloads instead of ~300.

Recommended fix priority: H5+B08 (HEAD-only pack, ~40–100× speedup) → H3 migration (B10, ~7–8×) → H4 (B08 batching).

**Fixtures:** `tests/fixtures/perf/case-study-{clone-baseline,tree-graph,server-objects}.json`
+ `build_case_study.py` (reproducible build script).

**Bonus:** `test_Perf__Case_Study__Vault.py` (16 tests) — structural regression suite
validating H1–H5 automatically. Beyond strict brief scope but accepted as high-value addition.

### Fixes applied

None — clean delivery.
